# mysql 5 配置   mysql 8需要时区配置
spring:
  datasource:
    username: root
    password: 789456
    url: jdbc:mysql://localhost:3306/news_app?useSSL=false&unicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: antidempic

#  jackson:
#    date-format: yyyy-MM-dd HH:mm:ss
#    time-zone: GMT-8

mybatis-plus:
  global-config:
    db-config:
      id-type: auto

server:
  port: 8081
